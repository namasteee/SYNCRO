# Syncro Prototype V5.8

React + Vite + Tailwind prototype for Syncro — another digital agency.

## Local setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The build output is generated in `dist/`.
